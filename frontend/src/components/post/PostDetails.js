import React from 'react';
import PostSummary from './PostSummary';
import PostTitle from './PostTitle';

const PostDetails = () => (
  <PostSummary>
    <PostTitle text="Aprenda a fazer TDD React com Redux." />
    <PostTitle text="Aprenda a fazer TDD React com Redux." />
  </PostSummary>
);

export default PostDetails;
